/**
 * Stub generated from the OpenAPI spec.
 * Replace with the output of `openapi-typescript` when the backend is ready.
 */
export type components = {
  schemas: {
    CreateClinicianRequest: {
      name: string;
      email: string;
      role: string;
      phone?: string;
    };
  };
};
